# 实现细节问答（供反向验证）

本文件逐条回答验证清单中的问题。所有结论均由源码直接对应，无推测。

## Q1. 网格是节点网格 `r_i = iΔr`，还是单元中心网格 `r_i=(i+½)Δr`？

**节点中心网格**：`r_i = i·Δr, i = 0..N`（共 **N+1 个节点**），即 N 个径向区间、
N+1 个节点、N+1 个节点中心控制体（首、末为半控制体）。
控制体界面取节点中点 `r_{i±½}`，并令 `r_{-½}=0`、`r_{N+½}=R₀`。
因此节点 `i=0` 恰在真实中心 `r=0`，节点 `i=N` 恰在真实表面 `r=R₀`。
这是“节点中心”格式，不是 `(i+½)Δr` 型单元中心网格。

对应源码：`grid.py`
```python
self.r = np.arange(self.N + 1) * self.dr
faces = _faces_from_nodes(self.r, self.R0)   # faces[0]=0, faces[-1]=R0
```

## Q2. `N=640` 表示多少区间、多少节点？

**N=640 表示 640 个径向区间、641 个节点、641 个节点中心控制体**（首末为半控制体）。
配置项为 `solver.radial_intervals = 640`（旧名 `grid_cells` 仍兼容）。
状态向量维度 `2*(N+1)`（N+1 个温度 + N+1 个含水率）。

## Q3. 表面节点是否对应真实的 `r=R`？

**是。** 节点 `N` 恰为 `r=R₀`（`dr·N = R₀`）。

## Q4. 是否加入半网格导热/传质阻力 `h_eff=(1/h + Δr/(2D))^{-1}`？

**没有加入，且不需要。** 本实现采用“**表面节点落在边界上**”的格式：末控制体的外
侧面恰好位于 `r=R₀`，而节点 `N` 就在该面上，因此表面 Robin 通量
`q_T = h_T(T_N−T_a)`、`q_C = h_C(C_N−C_a)` 直接作用于该节点；节点与表面之间不存在
额外的 `Δr/2` 串联导热/传质阻力，即 `h_eff = h`。

（若改用 `(i+½)Δr` 单元中心网格，则必须补上 `(1/h + Δr/(2D))^{-1}`；本实现不是这种网格。）

## Q5. 表面面积与末控制体体积如何计算？

单位轴向长度：
- 外表面面积 `A_{N+½} = 2πR₀`
- 内界面面积 `A_{N−½} = 2π·r_{N−½}`，其中 `r_{N−½} = R₀ − Δr/2`
- 末控制体体积 `V_N = π(R₀² − r_{N−½}²)`
- 中心界面 `A_{−½}=0`（故无需虚拟节点，也不用除以 `r`）

校验（`diagnostics.check_grid_geometry`）：`Σ V_i = πR₀²`、`A_{−½}=0`、`A_{N+½}=2πR₀`。

## Q6. 界面系数用调和平均还是算术平均？

**调和平均**，导热系数 `k` 与扩散系数 `D` 均用：
`a_{i+½} = 2 a_i a_{i+1} / (a_i + a_{i+1})`。
对应 `rhs.py` 的 `harmonic_mean`。

## Q7. 事件采用 `max_i C_i − 0.15`，还是径向连续插值后的最大值？

**用节点值 `g(t) = max_i C_i(t) − 0.15`（不插值）。**
`event.terminal = True`、`event.direction = −1.0`。
实测 `argmax` 恒为节点 0（中心），见 `event_trace_*.csv` 的 `argmax_index` 列。

> 严格性说明：临界时刻 `max C = 0.15`（数值上为 `0.15000000000000002`），并非严格小于；
> **该时刻之后**才满足 `max C < 0.15`。论文表述应为：“以全域最大含水率首次降至
> 0.15 kg/kg 的时刻作为理论临界烘干时间，其后全域严格低于 0.15。”若要整数秒安全
> 操作时间，取 `t₃,safe = 59287 s`、`t₄,safe = 70760 s`（不代替理论临界时间）。

## Q8. 问题 4 的事件最大值在材料坐标节点上求，还是固定物理输出点上求？

**在材料坐标（ξ）节点上求**，即 `g₄(t) = max_i c_i(t) − 0.15`。
固定物理输出点（0、0.1、…、2.0 cm）只用于写 Excel，不参与事件判定。

## Q9. `solve_ivp` 事件是直接终止，还是先按 60 s 输出再插值？

**直接终止**（`terminal=True`）。临界时刻先由 BDF 求解器在内部稠密插值上做根查找得到，
与 60 s 输出网格无关；60 s 只是事后采样。

**独立的 brentq 复核已真正执行**：事件后额外积分 150 s 以建立严格异号区间
`g(t_lo) > 0 > g(t_hi)`，再调用 `brentq`；`run_summary.json` 的 `q3_event/q4_event` 中
含 `brent_executed: true` 与括号值。复核根与求解器事件时刻一致（约 1e-9 s 量级）。

收敛性佐证（`space.csv` / `tolerance.csv` / `convergence_space.csv`）：
- 网格 N=160/320/640 → t*₃ = 59286.102 / 59286.175 / 59286.194 s；
  t*₄ = 70760.077 / 70759.979 / 70759.952 s
- 空间收敛阶 p₃≈1.98、p₄≈1.89（接近二阶）；Richardson 外推 t∞₃≈59286.200 s、t∞₄≈70759.942 s
- 容差 rtol=1e-7/1e-8/1e-9 → t* 变化 < 0.001 s
- **不确定度表述**：时间积分误差 < 0.002 s；综合空间离散后，N=320 的 t* 不确定度约
  **< 0.04 s**（不是“< 0.02 s”）。

## Q11. Q3/Q4 的水分收支如何检查？

`conservation_residuals.csv` 覆盖三组，均使用 12001 点梯形积分：
- `Q1_fixed_domain`：`dM/dt = -A_R h_C (C_s - C_a)`
- `Q3_fixed_domain`：同上（积到各自临界时刻）
- `Q4_material_reference_domain`：用**材料坐标**度量 `M_ξ(t)=Σ V_i^ξ c_i(t)`，
  `dM_ξ/dt = -(2π h_C / R(t))[c_s(t) − C_a(t)]`；**不使用**当前物理体积
  `R(t)²V_i^ξ` 积分含水率，以免把几何收缩误当成水分流失。

## Q10. 附件 1 中重复的 5280 s 记录如何取（删第一条/第二条/取平均）？

**实际附件 1 中不存在重复的 5280 s 记录。** 241 条时间严格递增
（`Counter` 无重复项），`read_attachment1` 强制 `np.diff(t) > 0`，一旦出现重复会
直接抛错而不会静默取舍。因此本项无需处理。

---

# 其他关键实现细节（主动补充）

- **问题 2/3 起点**：按 `四问统一建模与Claw执行规范.md` 与 `解题指导书_Claw执行版.md`，
  问题 2 从**统一初始场**（T=28 °C、C=2.55）独立起算，不承接问题 1 末态。
- **环境延拓**：`t ≤ 14400 s` 用附件 1 的分段线性插值；`t > 14400 s` 取附件 1
  **最后 1 h（10800–14400 s，共 61 点）均值** → `T_a=49.99893443 °C`、`C_a=0.04998754`。
- **半径**：附件 2 的 cm→m 用 `×0.01`；PCHIP 严格单调不增，范围
  `[0.01198, 0.02000] m`，无过冲；`t > 259200 s` 保持末值 `0.01198 m`。
- **终点原始值（未舍入，供反验证）**
  - `q3_event_time_s_raw = 59286.175088757955 s → 16.468381969 h`（→ 16.4684 h）
  - `q4_event_time_s_raw = 70759.97874952822 s → 19.655549653 h`（→ 19.6555 h）
  - 注意：Q4 未舍入秒数 **小于** 70759.98 s，故 4 位小数为 **19.6555 h**；
    若用 70760.0 反算才会得到 19.6556 h。
- **N=100 与 N=160/320/640 的 2–4 s 差异**：来源于空间离散与表面半控制体处理，
  而非终止事件；本实现终点对网格/容差均已收敛（见上）。
- **Excel 写入**：数值以 `float64` 写入并设 `number_format="0.0000"`，不做字符串化；
  `result4.xlsx` 中 `r_k > R(t)` 的位置留空，另设末列 `药材表面`。
